# k8s-crawlera-headless-browser
Configuration files for crawlera-headless-browser to run on k8s

Set up on k8s, and apps running selenium can reach the proxy on `crawlera-headless-browser.default:80`

